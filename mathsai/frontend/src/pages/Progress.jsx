/** Teaching history tracker — full UI implemented in Phase 6 (CLAUDE.md
 * Progress Page). The backend API is already complete (Phase 3). */
export default function Progress() {
  return <p className="p-8 text-text-secondary">Progress page — coming in Phase 6.</p>;
}
